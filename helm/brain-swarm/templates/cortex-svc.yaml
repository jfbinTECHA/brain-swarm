{{- if .Values.cortex.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "brain-swarm.fullname" . }}-cortex
spec:
  selector: { app: cortex }
  ports:
    - name: http
      port: 80
      targetPort: http
{{- end }}
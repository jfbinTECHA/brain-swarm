{{- if .Values.cortex.enabled }}
{{- if .Values.monitoring.prometheus.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "brain-swarm.fullname" . }}-alertmanager-config
  labels:
    {{- include "brain-swarm.labels" . | nindent 4 }}
    app.kubernetes.io/component: alertmanager-config
data:
  alertmanager.yaml: |
{{ .Files.Get "alertmanager/alertmanager.yaml" | indent 4 }}
{{- end }}
{{- end }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "local-admin.fullname" . }}
  labels:
    {{- include "local-admin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "local-admin.selectorLabels" . | nindent 4 }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "local-admin.fullname" . }}-dashboards
  labels:
    {{- include "local-admin.labels" . | nindent 4 }}
data:
  dashboards.json: |
    {{- toJson .Values.dashboards | nindent 4 }}
diff --git a/.env b/.env
index 1234567..89abcde 100644
--- a/.env
+++ b/.env
@@
 # === Cortex Summarizer Configuration ===
 REDIS_URL=redis://localhost:6379
 CHROMA_URL=http://localhost:8000
 DUCKDB_PATH=/home/sysop/brainswarm/data/cortex.duckdb
 S3_BUCKET=brainswarm-cortex

 # Optional fine-tuning
-SUMMARIZATION_INTERVAL=300               # Run summarization every 300s (5 min)
-EMBEDDING_MODEL=text-embedding-3-large
-CORTEX_MODE=live
+SUMMARIZATION_INTERVAL=300
+EMBEDDING_MODEL=text-embedding-3-large
+CORTEX_MODE=live
+
+# --- Cortex Summarizer Notes ---
+# Pydantic requires strict value types:
+#   • Integers must not include comments or extra spaces.
+#   • Use plain numeric values for timing parameters.
+#   • Leave comments on separate lines starting with '#'.
+# This prevents type parsing errors during service startup.